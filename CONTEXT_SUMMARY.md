# Content OS — Project Context Summary

**Last updated:** 2026-09-09 03:00  
**Repo:** `C:\dev\content-production`  
**Branch:** `main` (synced with `origin/main`)  
**Current state:** 5 commits pushed, R2 bucket created, deployment blocked on `CLOUDFLARE_API_TOKEN`

---

## 1. Current State

### Commits (all pushed to origin/main)
```
6b681f3 Fix React warning: KeyRow used value without onChange for uncontrolled fields
4cde627 Image generation: Workers AI + VyceAI providers, Settings key storage, studio chips
92c1a58 Fix cron trigger syntax in wrangler.toml
6c9ad23 Thumbnail Studio: fit right-side controls panel to viewport
062033c Fix D1 migrations; add R2, cron and Workers AI bindings
3864ce1 Thumbnail Studio: external image-gen flow and fit-to-viewport layout
1b7f550 fix: correct aspect ratios and move variant slots to filmstrip
d4a939d Added Thumbnail Studio screen
```

### Working tree
- Clean (only `worker-configuration.d.ts` is untracked — auto-generated by `npx wrangler types`)
- **No uncommitted changes**

### Cloudflare Infrastructure
| Resource | Status |
|---|---|
| D1 database `content-os-db` | ✅ Bound (id: `25b209b2...`) |
| KV namespace (id: `e9050d4f...`) | ✅ Bound |
| R2 bucket `content-os-media` | ✅ Created (via dashboard) |
| Workers AI (`[ai]` binding) | ✅ Bound |
| Cron triggers (0 9 * * *, 0 */3 * * *) | ✅ Configured |

### Local D1 (development)
- All 6 migrations applied locally
- Local DB at `.wrangler/state/v3/d1/miniflare-D1DatabaseObject/<hash>.sqlite`
- Tables: `library` (12 cols), `post_performance` (10 cols), `projects` (7 cols), `activity`, `resources`, `telegram_tasks`, `workspace`

---

## 2. What Was Built

### Thumbnail Studio (`src/components/aios/sections/ThumbnailStudioScreen.tsx`)
- Left: Canvas with aspect-ratio toggle (16:9 YouTube / 9:16 Reels)
- Right panel (fit-to-viewport, no scroll): prompt, style presets, provider chips, Generate button, 3 layer toggles, URL input row
- **4 variant tiles** below canvas: click → file picker, drag-and-drop, ✕ clear, selected tile becomes canvas background
- **5 provider chips** (SITES array, index 0):
  - `workers-ai` — in-app, free, no key needed (Workers AI Flux models)
  - `vyceai` — in-app, requires key from Settings → API Keys → Image Generation
  - `gemini` — opens new tab (external)
  - `chatgpt` — opens new tab (requires Plus)
  - `arena.ai` — opens new tab (external, free)
- **handleGenerate()** branches:
  - `workers-ai` / `vyceai`: POST `/api/generate-image` → load result into tile
  - `gemini` / `chatgpt` / `arena`: clipboard copy prompt → open new tab
- Workers AI model Select only visible when `site === "workers-ai"` (picks from 3 whitelisted models, default from Settings)
- "Generating…" button state + disabled during in-app generation
- Empty prompt → "Write a prompt first" error
- Live text layer ("YOUR HEADLINE") always editable on canvas

### Settings Page (`src/components/aios/sections/Settings.tsx`)
- **API Keys tab** — Image Generation section added between "AI Brain" and "Scrapers — Apify"
  - Workers AI — Model Select (3 options + "No key required" badge)
  - VyceAI — API Key (masked KeyRow, shows "Configured (...last4)" or "Not configured")
  - VyceAI — Model badge "grok-imagine-2" (fixed, not editable)
  - "Save image generation" button → POST `/api/settings-imagegen`
  - Warning: "Image-generation keys are saved to Cloudflare KV..."
- **Other sections** (Apify slots, Data Sources, Telegram, etc.) — UI present, save NOT wired yet (future work)
- KeyRow component now handles both controlled (`value`+`onChange`) and uncontrolled (`defaultValue`) modes

### API Routes

#### `src/routes/api/generate-image.ts` (POST)
- Zod schema: `{ prompt, provider, model? }`
- PROVIDERS registry:
  - `workers-ai`: `env.AI.run(model, { prompt })` → Blob → base64 data URL. Returns `{ ok, url, provider, model }`. Local dev returns "deploy only" error since `env.AI` undefined in vite.
  - `vyceai`: `POST https://vyceai.com/v1/images/generations` with `Authorization: Bearer <key>`. Key resolution: KV `settings:imagegen:vyceai` first, then `env.VYCE_API_KEY` secret fallback. Returns `{ ok, url, provider, model }`.
- Model whitelist enforced: `@cf/black-forest-labs/flux-2-klein-4b` (default), `@cf/black-forest-labs/flux-2-dev`, `@cf/black-forest-labs/flux-2-klein-9b`
- VyceAI model: `grok-imagine-2` (hardcoded, OpenAI-compatible)
- NEVER logs/echoes raw API key

#### `src/routes/api/settings-imagegen.ts` (GET/POST)
- GET: returns `{ vyceai: { configured, last4 }, defaultModel, allowedModels }`
- POST: accepts `{ vyceaiKey?, clearVyceai?, defaultModel? }` — writes to KV keys `settings:imagegen:vyceai` and `settings:imagegen:default-model`
- Uses sonner toast for success/error feedback

---

## 3. What's NOT Yet Done (Phase 7+)

| Item | Status |
|---|---|
| **Deploy** | ❌ Blocked — needs `CLOUDFLARE_API_TOKEN` |
| **Live testing** | ❌ Can't test until deployed |
| **Scrapers (8 sites)** | ⏳ Phase 6 — not started |
| **Telegram morning brief** | ⏳ Phase 9 — not started |
| **Full Settings save** (Apify slots, Data Sources, Notifications) | ⏳ Phase 7 — not started |
| **⌘K command palette** | ⏳ Phase 4 — not started |
| **R2 bucket upload** | ⏳ Will auto-work once deployed |

---

## 4. Blockers & Requirements

### CLOUDFLARE_API_TOKEN (CRITICAL — blocks everything)
- Needed for: `wrangler deploy`, `wrangler r2 bucket create` (retry), `wrangler secret put`
- Token must have: **Workers Scripts:Edit** + **R2:Edit** permissions
- **NOT** the same as the OAuth login — OAuth is read-only for write operations
- After setting: `$env:CLOUDFLARE_API_TOKEN = "your-token"` (PowerShell)

### API Keys to add (in Settings UI or via wrangler secret)
| Key | Location | Purpose |
|---|---|---|
| `VYCE_API_KEY` | `wrangler secret put VYCE_API_KEY` OR Settings → API Keys → Image Generation → VyceAI field | Used as KV fallback for VyceAI generation |
| (future) Apify tokens | Settings → Scrapers — Apify slots | Per-competitor scraping |
| (future) YouTube, SerpApi, Reddit | Settings → Data Sources | Content research |
| (future) Telegram Bot Token | Settings → Notifications | Morning brief delivery |

---

## 5. Next Steps (in order)

### Step 1: Unblock deploy (5 min)
1. Create API token at https://dash.cloudflare.com/profile/api-tokens
2. Permissions: Workers Scripts:Edit + R2:Edit
3. Set in PowerShell: `$env:CLOUDFLARE_API_TOKEN = "token"`
4. Run: `npx wrangler deploy`
5. Save the live URL (e.g., `https://content-production.<account>.workers.dev`)

### Step 2: Set VyceAI key (2 min)
- Go to deployed Settings → API Keys → Image Generation
- Select Workers AI model (default: flux-2-klein-4b)
- Paste VyceAI API key (`sk-...`) in VyceAI field
- Click "Save image generation" — should see "Configured (...last4)"

### Step 3: Test image generation (3 min)
- Go to `/thumbnail-studio`
- Select **VyceAI (Custom)** chip
- Type prompt (≤2000 chars)
- Click "Generate background"
- Image should appear in tile 0 and as canvas background within seconds

### Step 4: Test Workers AI (2 min)
- Select **Workers AI** chip
- Type prompt
- Click "Generate background"
- Image should appear in tile (free, no key needed)

### Step 5: Push remaining Settings sections (Phase 7)
- Wire save to Apify slots, Data Sources, Telegram notifications
- Each section gets its own `/api/settings-*` route + KV key

### Step 6: Build scrapers (Phase 6)
- 8 sites (Reddit, Product Hunt, etc.)
- Use existing Apify tokens from `keys.txt`
- Schedule via existing cron triggers

### Step 7: Telegram morning brief (Phase 9)
- 0 9 * * * cron already configured
- Needs: scrape → quality score → ideator → compose → send via Telegram Bot API

---

## 6. Key Files Reference

| File | Purpose |
|---|---|
| `src/components/aios/sections/ThumbnailStudioScreen.tsx` | Main thumbnail editor (canvas + controls) |
| `src/components/aios/sections/Settings.tsx` | Settings page (API keys, scrapers, etc.) |
| `src/routes/api/generate-image.ts` | In-app image generation route |
| `src/routes/api/settings-imagegen.ts` | Settings KV read/write route |
| `wrangler.toml` | Cloudflare bindings config |
| `migrations/*.sql` | D1 schema (6 files, all applied locally) |
| `package.json` | Dependencies (no new ones added for this work) |
| `src/routes/api/scrape-competitor.ts` | Example route pattern for future scrapers |

---

## 7. Architecture Notes

- **Framework:** TanStack Start + React 19 + Tailwind v4 + Nitro SSR
- **Runtime:** Cloudflare Workers (not Pages — Workers project confirmed)
- **Storage:** D1 (relational SQL), KV (key-value, used for settings), R2 (object storage)
- **AI:** Workers AI (in-app, free), VyceAI (custom OpenAI-compatible)
- **Build:** `npm run build` → produces `.output/` (Nitro server + client)
- **Dev:** `npm run dev` (Vite, port 8080) or `npm run cf:dev` (Wrangler local worker)
- **Note:** Workers AI `env.AI` only works in deployed Worker, not in `vite dev` or `wrangler dev`

---

## 8. User's Key Storage

External API keys stored locally in:
- `C:\dev\keys.txt` — raw key values (NOT in git, NOT in repo)
- Cloudflare KV (`settings:imagegen:vyceai`) — after saving via Settings UI
- Cloudflare Secrets (`VYCE_API_KEY`) — optional env fallback

---

*This file is auto-generated. Do not edit directly — regenerate when project state changes.*
